#/bin/bash

## Launcher for CloudOpting-Crane

# add parameters to select:
# - domain
# - disable registry and remote deploy
# - debug mode

# check:
# - local docker running
# - registry -> install if not installed, reconfigure if needed.
# - run commander, do the built-in check and parse response
